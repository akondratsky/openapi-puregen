/**
 * @description Since I'm not sure how it should work when working with global npm module, f.e.,
 * I've created this separate function.
 * @param path path resolve
 * @returns {string} representates resolved path (relative reduced to absolute)
 */
export const resolvePath = (path: string): string => {
  return path;
};
